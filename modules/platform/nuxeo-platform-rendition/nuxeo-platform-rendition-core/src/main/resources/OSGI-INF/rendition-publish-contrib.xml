<?xml version="1.0"?>
<component name="org.nuxeo.ecm.platform.rendition.publish.contrib">

  <extension target="org.nuxeo.ecm.platform.rendition.service.RenditionService"
    point="defaultRendition">
    <defaultRendition reason="publish">
      <script language="JavaScript">
        function run() {
          if (Document.getType() == "File" || Document.getType() == "Note") {
            return 'pdf';
          } else if (Document.hasSchema("file")) {
            return 'mainBlob';
          }
          return null;
        }
      </script>
    </defaultRendition>
  </extension>

  <extension target="org.nuxeo.ecm.platform.query.api.PageProviderService" point="providers">
    <coreQueryPageProvider name="ALL_PUBLICATION_QUERY">
      <pattern>
        SELECT * FROM Document WHERE ecm:isProxy = 1 AND (rend:sourceVersionableId = ? OR ecm:proxyVersionableId = ?)
      </pattern>
      <pageSize>0</pageSize>
      <maxPageSize>0</maxPageSize>
    </coreQueryPageProvider>
  </extension>

</component>
