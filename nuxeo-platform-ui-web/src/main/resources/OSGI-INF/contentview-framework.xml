<?xml version="1.0"?>

<component name="org.nuxeo.ecm.platform.ui.web.ContentViewService">

  <documentation>
    The Content View services provides extension points for content view
    registration.

    @author Anahide Tchertchian (at@nuxeo.com)
  </documentation>

  <implementation
    class="org.nuxeo.ecm.platform.ui.web.contentview.ContentViewServiceImpl" />

  <service>
    <provide
      interface="org.nuxeo.ecm.platform.ui.web.contentview.ContentViewService" />
  </service>

  <extension-point name="contentViews">
    <documentation>
      Content Views are referencing resources that will help build a listing on
      the UI.
    </documentation>
    <object
      class="org.nuxeo.ecm.platform.ui.web.contentview.ContentViewDescriptor" />
  </extension-point>

  <extension target="org.nuxeo.ecm.platform.ui.web.ContentViewService"
    point="contentViews">

    <contentView name="CURRENT_DOCUMENT_CHILDREN">

      <coreQueryPageProvider>
        <pageSize>2</pageSize>
        <property name="coreSession">#{documentManager}</property>
        <property name="checkQueryCache">true</property>
        <pattern>
          SELECT * FROM Document WHERE ecm:parentId = ? AND
          ecm:isCheckedInVersion = 0 AND ecm:mixinType != 'HiddenInNavigation'
          AND ecm:currentLifeCycleState != 'deleted'
        </pattern>
        <sort column="dc:title" ascending="true" />
      </coreQueryPageProvider>

      <selectionList>CURRENT_SELECTION</selectionList>
      <pagination>simple</pagination>
      <actions category="CURRENT_SELECTION_LIST" />
      <actions category="CURRENT_SELECTION_LIST" />
      <resultLayout>document_listing</resultLayout>
      <!-- <cacheKey>#{currentDocument.cacheKey}</cacheKey> -->
      <refresh>
        <event>documentChildrenChanged</event>
      </refresh>
    </contentView>

    <contentView name="coreQueryAndFetch">
      <coreQueryPageProvider>
        <property name="coreSession">#{documentManager}</property>
        <pattern>
          SELECT * FROM Document WHERE ecm:primaryType = 'Section' AND
          ecm:mixinType != 'HiddenInNavigation' AND ecm:path STARTSWITH ?
        </pattern>
      </coreQueryPageProvider>
    </contentView>

  </extension>

</component>
