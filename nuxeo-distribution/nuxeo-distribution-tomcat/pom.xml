<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.nuxeo.ecm.distribution</groupId>
    <artifactId>nuxeo-distribution</artifactId>
    <version>5.4.3-I20111024_2229</version>
    <relativePath>../pom.xml</relativePath>
  </parent>

  <artifactId>nuxeo-distribution-tomcat</artifactId>
  <packaging>pom</packaging>
  <name>Nuxeo Tomcat Build</name>
  <description>Package Nuxeo CAP or DM WAR with Tomcat</description>

  <dependencies>
    <dependency>
      <groupId>org.nuxeo.ecm.platform</groupId>
      <artifactId>nuxeo-platform-seam-debug</artifactId>
    </dependency>
    <dependency>
      <groupId>org.nuxeo.runtime</groupId>
      <artifactId>nuxeo-runtime-jtajca</artifactId>
    </dependency>
    <dependency>
      <groupId>org.nuxeo.runtime</groupId>
      <artifactId>nuxeo-runtime-datasource</artifactId>
    </dependency>
    <dependency>
      <groupId>org.nuxeo.runtime</groupId>
      <artifactId>nuxeo-runtime-tomcat-adapter</artifactId>
    </dependency>
    <dependency>
      <groupId>org.nuxeo.ecm.platform</groupId>
      <artifactId>nuxeo-platform-ws-sun-jaxws</artifactId>
    </dependency>
  </dependencies>

  <profiles>
    <profile>
      <id>all-distributions</id>
      <!-- profile for building all classifiers -->
      <properties>
        <profile.name>dm</profile.name>
      </properties>
    </profile>
    <profile>
      <id>all</id>
      <!-- profile for building all classifiers -->
      <activation>
        <activeByDefault>true</activeByDefault>
      </activation>
      <properties>
        <profile.name>dm</profile.name>
      </properties>
    </profile>
    <profile>
      <id>nuxeo-coreserver</id>
      <!-- profile for building Tomcat with only Nuxeo CoreServer -->
      <properties>
	<profile.name>coreserver</profile.name>
      </properties>
    </profile>
    <profile>
      <id>nuxeo-cap</id>
      <properties>
	<profile.name>cap</profile.name>
      </properties>
      <!-- profile for building Tomcat with only Nuxeo CAP -->
    </profile>
    <profile>
      <id>nuxeo-dm</id>
      <properties>
	<profile.name>dm</profile.name>
      </properties>
      <!-- default profile for building Tomcat with only Nuxeo DM -->
    </profile>
    <profile>
      <id>sdk</id>
      <!-- profile for building SDK distributions (they are always built when release
        profile is activated) -->
    </profile>
  </profiles>

  <build>
    <plugins>
      <plugin>
	<groupId>org.jboss.tattletale</groupId>
	<artifactId>tattletale-maven</artifactId>
	<configuration> 
	  <!--source>${project.build.directory}/nuxeo-${profile.name}-${project.version}-tomcat/nxserver/lib:${project.build.directory}/nuxeo-${profile.name}-${project.version}-tomcat/lib</source--> 
	  <source>${project.build.directory}/nuxeo-${profile.name}-${project.version}-tomcat/nxserver/lib</source>
	</configuration>
      </plugin>
      <plugin>
        <groupId>org.nuxeo.build</groupId>
        <artifactId>nuxeo-distribution-tools</artifactId>
        <configuration>
          <buildFile>${basedir}/src/main/assemble/assembly.xml</buildFile>
        </configuration>
        <executions>
          <execution>
            <id>packaging</id>
            <goals>
              <goal>build</goal>
            </goals>
            <phase>package</phase>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

</project>
