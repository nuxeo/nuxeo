In order to run these integration tests, a Nuxeo DM instance must be running
(for now) on http://localhost:8080/nuxeo

Then run the integration tests with:
  mvn verify

Test results are available in target/failsafe-reports
