<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.nuxeo.ecm.core</groupId>
    <artifactId>nuxeo-core-parent</artifactId>
    <version>1.5-SNAPSHOT</version>
  </parent>

  <artifactId>nuxeo-core-storage-sql-ra${artifactSuffix}</artifactId>
  <packaging>rar</packaging>
  <name>Nuxeo Core SQL Storage Resource Adapter</name>
  <description>Nuxeo Core: SQL Storage Resource Adapter.</description>

  <repositories>
    <repository>
      <id>glassfish-repository</id>
      <url>http://download.java.net/maven/glassfish</url>
    </repository>
    <repository>
      <id>download.java.net</id>
      <url>http://download.java.net/maven/2</url>
    </repository>
  </repositories>

  <dependencies>
    <dependency>
      <groupId>org.nuxeo.common</groupId>
      <artifactId>nuxeo-common</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.nuxeo.ecm.core</groupId>
      <artifactId>nuxeo-core-storage-sql</artifactId>
      <version>${version}</version>
      <scope>provided</scope>
    </dependency>

    <!-- TODO: Use the dep from the central repo -->
    <dependency>
      <groupId>javax.ressource</groupId>
      <artifactId>jca-api</artifactId>
      <version>1.5</version>
      <scope>provided</scope>
    </dependency>

    <dependency>
      <groupId>log4j</groupId>
      <artifactId>log4j</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.derby</groupId>
      <artifactId>derby</artifactId>
      <version>10.2.2.0</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.glassfish.distributions</groupId>
      <artifactId>web-all</artifactId>
      <version>10.0-build-20080430</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.glassfish.embedded</groupId>
      <artifactId>gf-embedded-api</artifactId>
      <version>1.0-alpha-5-NX1</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <profiles>
    <profile>
      <id>jboss</id>
      <properties>
        <raDir>ra-jboss</raDir>
        <artifactSuffix></artifactSuffix>
      </properties>
    </profile>
    <profile>
      <id>glassfish</id>
      <activation>
        <activeByDefault>true</activeByDefault>
      </activation>
      <properties>
        <raDir>ra-glassfish</raDir>
        <artifactSuffix>-jee</artifactSuffix>
      </properties>
    </profile>
  </profiles>

  <build>
    <finalName>${pom.artifactId}-${pom.version}</finalName>

    <resources>
      <resource>
        <directory>src/main/resources/${raDir}</directory>
      </resource>
      <resource>
        <directory>src/main/resources</directory>
        <filtering>true</filtering>
        <excludes>
          <exclude>ra-*/**</exclude>
        </excludes>
      </resource>
    </resources>

    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-rar-plugin</artifactId>
        <version>2.2</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>rar</goal>
            </goals>
            <configuration>
              <rarSourceDirectory>${project.build.outputDirectory}</rarSourceDirectory> <!-- include class files -->
              <raXmlFile>src/main/resources/${raDir}/META-INF/ra.xml</raXmlFile>
              <manifestFile>${project.build.directory}/classes/META-INF/MANIFEST.MF</manifestFile>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-antrun-plugin</artifactId>
        <version>1.1</version>
        <executions>
          <execution>
            <phase>test-compile</phase>
            <goals>
              <goal>run</goal>
            </goals>
            <configuration>
              <tasks>
                <property name="maven_test_classpath" refid="maven.test.classpath" />
                <property name="target" value="${basedir}/target/test-servlet-classes" />
                <delete dir="${target}" />
                <mkdir dir="${target}" />
                <javac srcdir="src/test-servlet/java" destdir="${target}"
                       classpath="${maven_test_classpath}" />
              </tasks>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

</project>
