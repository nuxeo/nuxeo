<datasources>
  <local-tx-datasource>
    <jndi-name>NuxeoDS</jndi-name>
    <connection-url>jdbc:oracle:thin:@localhost:1521:XE</connection-url>
    <driver-class>oracle.jdbc.driver.OracleDriver</driver-class>
    <user-name>NUXEO</user-name>
    <password>nuxeo</password>
    <!-- postgresql supports empty statement evaluation to cheaply
         ensure the connection is still up before using it
    <check-valid-connection-sql>;</check-valid-connection-sql>
    -->
  </local-tx-datasource>

</datasources>