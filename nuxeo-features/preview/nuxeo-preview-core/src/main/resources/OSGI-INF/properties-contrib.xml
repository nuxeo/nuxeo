<?xml version="1.0"?>
<component name="org.nuxeo.ecm.platform.preview.properties">

  <extension target="org.nuxeo.runtime.ConfigurationService" point="configuration">
    <documentation>
      Property that enables the old JSF preview that allows annotations to be set.
    </documentation>
    <property name="nuxeo.old.jsf.preview">false</property>

    <documentation>
      Property that enables or disables zip files preview.
    </documentation>
    <property name="nuxeo.preview.zip.enabled">false</property>

    <documentation>
      Property that enables the use of the generic HTML converter if no converter is found
      for a given source mime type. Defaults to true.
      @since 9.10-HF21
    </documentation>
    <property name="nuxeo.preview.allowGenericHTMLConverter">true</property>
  </extension>

</component>
