<?xml version="1.0"?>

<component name="org.nuxeo.ecm.platform.routing.querymodel">
 
  <extension
    target="org.nuxeo.ecm.core.search.api.client.querymodel.QueryModelService"
    point="model">

    <queryModel name="CURRENT_DOC_ROUTING_RELATION_SEARCH">
      <pattern>
        SELECT * FROM Document WHERE ecm:mixinType != 'Folderish'
        AND ecm:currentLifeCycleState != 'deleted' AND ecm:path STARTSWITH ? 
        AND dc:title STARTSWITH ?
      </pattern>
      <sortable value="true" defaultSortColumn="dc:title"
        defaultSortAscending="true" />
      <max>20</max>
    </queryModel>
    
  </extension>

</component>
