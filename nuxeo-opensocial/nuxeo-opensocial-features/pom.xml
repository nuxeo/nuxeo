<?xml version="1.0"?>
<project>
 <parent>
    <groupId>org.nuxeo.opensocial</groupId>
    <artifactId>nuxeo-opensocial-parent</artifactId>
    <version>1.0-SNAPSHOT</version>
  </parent>
  <modelVersion>4.0.0</modelVersion>
  <groupId>org.nuxeo.opensocial</groupId>
  <artifactId>nuxeo-opensocial-features</artifactId>
  <name>Nuxeo OpenSocial :: Features</name>
  <description>Opensocial Features</description>
  <packaging>jar</packaging>

  <build>
    <plugins>


      <plugin>
            <!--
              TODO: Replace this with the more generic javascript plugin that
              allows the use of arbitrary compressor / compilers. The
              maven-javascript-plugin does not seem to work.
            -->
            <!-- <groupId>net.sf.hammerfest</groupId> -->
            <!-- <artifactId>maven-javascript-plugin</artifactId> -->
            <groupId>net.sf.alchim</groupId>
            <artifactId>yuicompressor-maven-plugin
            </artifactId>
            <executions>
              <execution>
                <goals>
                  <goal>compress</goal>
                </goals>
              </execution>
            </executions>
            <configuration>
              <suffix>.opt</suffix>
              <excludes>
                <exclude>target/**</exclude>
                <!-- don't bother with other file types. -->
                <exclude>**/*.xml</exclude>
                <exclude>**/*.html</exclude>
                <exclude>**/*.gif</exclude>
                <exclude>**/*.jpeg</exclude>
                <exclude>**/*.png</exclude>

              </excludes>
            </configuration>
          </plugin>


    </plugins>


    <resources>
      <resource>
        <targetPath>nxfeatures</targetPath>
        <directory>${basedir}/src/main/js</directory>
      </resource>
      <resource>
        <directory>${basedir}/src/main/resources</directory>
      </resource>
    </resources>

  </build>

  <dependencies>

    <dependency>
      <groupId>org.apache.shindig</groupId>
      <artifactId>shindig-features</artifactId>
      <version>${shindig.version}</version>
    </dependency>


  </dependencies>

</project>
