<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.nuxeo.ecm.platform</groupId>
    <artifactId>nuxeo-application-distribution</artifactId>
    <version>5.2-20081007</version>
  </parent>

  <artifactId>nuxeo-shell-template</artifactId>
  <packaging>pom</packaging>
  <name>Nuxeo Shell Template</name>
  <description>The distribution template for nuxeo shell</description>


  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-nuxeo-plugin</artifactId>
        <extensions>true</extensions>
        <executions>
          <execution>
            <id>assemble-app</id>
            <phase>package</phase>
            <goals>
              <goal>assembly</goal>
            </goals>
            <configuration>
              <runPreprocessor>false</runPreprocessor>
              <!--format>directory</format-->
              <format>zip</format>
	      <zipRoot>nxshell</zipRoot>
              <outputDirectory>target</outputDirectory>
              <targetFile>${application.name}</targetFile>
              <descriptor>
                ${basedir}/src/main/assemble/assembly.xml
              </descriptor>
            </configuration>
          </execution>
          <execution>
            <id>attach-app</id>
            <phase>package</phase>
            <goals>
              <goal>attach</goal>
            </goals>
            <configuration>
              <file>${basedir}/target/${application.name}.zip</file>
              <type>zip</type>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

</project>
