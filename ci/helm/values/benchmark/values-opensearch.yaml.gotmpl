resources:
  requests:
    cpu: "15"
    memory: "56Gi"
  limits:
    cpu: "15"
    memory: "56Gi"
opensearchJavaOpts: "-Xmx32g -Xms32g"
podAnnotations:
  ad.datadoghq.com/elasticsearch.checks: |
    {
      "elastic": {
        "init_config": {},
        "instances": [
          {
            "url": "http://%%host%%:9200"
          }
        ]
      }
    }
  ad.datadoghq.com/tags: '{"source": "opensearch", "service":"{{ env "SERVICE_TAG" | default "benchmark-local" }}"}'
labels:
  usage: benchmark
  app: opensearch
service:
  labels:
    usage: benchmark
tolerations:
- key: team
  operator: Equal
  value: platform
  effect: NoSchedule
- key: usage
  operator: Equal
  value: benchmark
  effect: NoSchedule
nodeSelector:
  team: platform
  usage: benchmark
