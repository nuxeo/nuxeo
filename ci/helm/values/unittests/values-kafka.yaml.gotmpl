podLabels:
  dev.nuxeo.com/usage: utests
{{ tpl (readFile "../values-kafka-extra-deploy.yaml") (dict "chartName" "kafka") }}
zookeeper:
  podLabels:
    dev.nuxeo.com/usage: utests
  {{ tpl (readFile "../values-kafka-extra-deploy.yaml") (dict "chartName" "zookeeper") | nindent 2 }}
