<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.nuxeo.runtime</groupId>
    <artifactId>nuxeo-runtime-parent</artifactId>
    <version>1.7.0-SNAPSHOT</version>
  </parent>

  <groupId>org.nuxeo.runtime</groupId>
  <artifactId>nuxeo-runtime-osgi</artifactId>
  <name>Nuxeo Runtime OSGI Adapter</name>
  <description>Nuxeo Runtime: OSGi Framework Adapter.</description>

  <dependencies>    
    <dependency>
      <groupId>org.nuxeo.common</groupId>
      <artifactId>nuxeo-common</artifactId>
    </dependency>

    <dependency>
      <groupId>org.osgi</groupId>
      <artifactId>osgi-core</artifactId>
    </dependency>
    <dependency>
      <groupId>jboss</groupId>
      <artifactId>jboss-system</artifactId>
    </dependency>
    <dependency>
      <groupId>jboss</groupId>
      <artifactId>jboss-common</artifactId>
    </dependency>
    <dependency>
      <groupId>jboss</groupId>
      <artifactId>jboss-jmx</artifactId>
    </dependency>
  </dependencies>
  <profiles>
    <profile>
      <id>bundlor</id>
      <build>
	<plugins>
	  <plugin>
	    <groupId>com.springsource.bundlor</groupId>
	    <artifactId>com.springsource.bundlor.maven</artifactId>
	    <version>1.0.0.RELEASE</version>
	    <executions>
              <execution>
		<id>bundlor</id>
		<goals>
		  <goal>bundlor</goal>
		</goals>
              </execution>
	    </executions>
	    <configuration>
	      <manifestTemplate>
Bundle-ManifestVersion: 2
Bundle-Name: ${project.name}
Bundle-Version: ${bundle.version}
Export-Template:
 org.nuxeo.*;version="${bundle.version}"
Import-Template: 
 org.nuxeo.*;version="${bundle.version.range}"
Import-Package: org.jboss.system.server,
 org.jboss.mx.util,
 org.jboss.logging,
 org.nuxeo.common,
 org.nuxeo.common.utils
              </manifestTemplate>
	      <manifestTemplatePath>${basedir}/src/main/resources/META-INF/MANIFEST.MF</manifestTemplatePath>
	      <outputPath>${basedir}</outputPath>
	    </configuration>
	  </plugin>
	</plugins>
      </build>
    </profile>
  </profiles>

</project>
